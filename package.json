{
  "name": "convert-units-wrapper",
  "version": "1.0.0",
  "description": "This is a thin wrapper that takes and emits coneverted units and uses a fixed precision",
  "main": "out/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@types/convert-units": "^2.3.3"
  }
}
